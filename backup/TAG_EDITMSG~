Features:

  - Importação de Produtos do MySQL para MongoDB

Deploy steps:

  - sudo su -
  - cd /etc/init.d/
  - vim redis_resque
  - >> copiar o conteudo do arquivo localizado em: raiz-do-projeto/doc/redis/redis_resque
  - chmod +x redis_resque
  - chkconfig --add redis_resque
  - chkconfig --level 2345 redis_resque on
  - cd /abd/app/cap-producs/current
  - rvm use --default
  - bundle install
  - reboot >> ou >> /etc/init.d/redis_resque start

# Please enter the log message for your tag. Lines starting
# with '#' will be ignored, and an empty message aborts the operation.
# You are about to increase the 'minor' version level.
